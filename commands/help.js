const Discord = require('discord.js');

module.exports.run = async (bot, message, args) => {
    message.channel.send('Oi cunt this command isn\'t done yet. The only command you can use right now is ?ping anyway so.');
}

module.exports.help = {
    name: "help"
}